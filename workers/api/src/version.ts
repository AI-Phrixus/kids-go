/**
 * Single source of truth for the deployed version string.
 * Synced from package.json by `scripts/sync-version.mjs` (runs before
 * build/deploy) — do not edit the value by hand.
 */
export const APP_VERSION = "0.8.0";
